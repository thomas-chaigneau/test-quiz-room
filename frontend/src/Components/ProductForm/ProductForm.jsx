import { useState } from 'react';
import './ProductForm.css'

const ProductForm = ({name, price, description, imageUrl}) => {
    // State
    //const [newProduct, setNewProduct] = useState();

    // Comportement
    //const handleNewProduct = () => {
        // set NewProduct({name, price, description, imageUrl})
        // post request : newProduct
    // }

    // Affichage
    return (
        <div>
            
        </div>
    )

}

export default ProductForm;
